<div class="hero-unit">
  <h2>Все новости</h2>
  <%- linkTo('Создать новую новость', addNewsPath, {class: 'btn pull-right'}); %>
</div>

<div id="news-list">
<% if (news) { %>
<% for (var i = 0, ii = news.length; i < ii; i++) { %>
  <div class="row list-item" id="news-<%= news[i].id; %>">
    <div class="span8">
    	<h3><a href="/news/edit/<%= news[i].id; %>"><%= news[i].title; %></a></h3>
    	<p>
    		<%= news[i].anonce; %>
    	</p>
    </div>
    <div class="span4">
      <h3><i class="icon-list-alt"></i><%= news[i].id; %></h3>
    </div>
  </div>
<% } %>
<% } %>
</div>
