<div class="hero-unit">
  <form id="news-form" class="form-horizontal" action="/news/update/<%= params.id %>" method="POST">
    <fieldset>
      <legend>Редактирование новоти</legend>
      <% if(params.errors) { %>
      <div class="control-group">
        <ul>
        <% for(var err in params.errors) { %>
          <li><%= params.errors[err]; %></li>
        <% } %>
        </ul>
      </div>
      <% } %>

      <%- partial('form', {news: news}) %>

      <div class="form-actions">
        <%- contentTag('input', 'Изменить', {type: 'submit', class: 'btn btn-primary'}) %>
        <%- contentTag('button', 'Отменить', {type: 'submit', formaction: '/news/', formmethod: 'GET', class: 'btn btn-primary'}) %>
        <%- contentTag('button', 'Удалить', {type: 'submit', formaction: '/news/destroy/' + params.id, formmethod: 'POST', class: 'btn btn-danger'}) %>
      </div>
    </fieldset>
  </form>
</div>
