<!-- Lets talk about geddy -->
<%- styleLink('/css/auth-buttons.css', {rel:'stylesheet'}) %>
<div class="hero-unit">

  <p>
    <form id="login-form" class="form-horizontal" action="/auth/authenticate" method="POST">
    <fieldset>
      <legend>Авторизация</legend>
      <div class="control-group">
        <label for="username" class="control-label">Пользователь</label>
        <div class="controls">
          <%- contentTag('input', params.username, {type:'text', class:'span6', name:'username'}) %>
        </div>
      </div>
      <div class="control-group">
        <label for="password" class="control-label">Пароль</label>
        <div class="controls">
          <%- contentTag('input', params.password, {type:'password', class:'span6', name:'password'}) %>
        </div>
      </div>
      <div class="form-actions">
        <%- contentTag('input', 'Войти', {type: 'submit', class: 'btn btn-primary'}) %>
      </div>
    </fieldset>
    </form>
  </p>

</div>
